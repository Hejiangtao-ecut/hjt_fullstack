function squareDigits(num){
    //may the code be with you
    let s = num.toString();
    let ss = "";
    for(i=0 ;i<s.length ;i++){
      let sss = s[i];
      sss.parseInt;
      ss = ss + (sss*sss).toString;
    }
    console.log(ss);
  }
  squareDigits(81);
